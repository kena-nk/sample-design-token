{
  "license": "UNLICENSED",
  "scripts": {
    "token:getJson": "./node_modules/.bin/token-transformer data/tokens.json output.json",
    "build:tokens": "style-dictionary build --config style-dictionary.config.json",
    "build:figma": "node ./scripts/figma.js",
    "token": "run-s token:getJson build:tokens"
  },
  "devDependencies": {
    "token-transformer": "^0.0.20"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "node-fetch": "^3.2.4",
    "style-dictionary": "^3.7.0",
    "yarn-run-all": "^3.1.1"
  }
}
